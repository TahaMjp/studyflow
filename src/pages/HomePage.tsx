/**
 * HomePage.tsx
 *
 * Landing page of the StudyFlow application.
 * This is currently a placeholder component
 * to prevent routing and TypeScript errors.
 */

import React from "react";

/**
 * HomePage Component
 * Displays basic placeholder content.
 */
const HomePage: React.FC = () => {
  return (
    <section className="flex items-center justify-center min-h-[60vh]">
      <h1 className="text-4xl font-bold">Welcome to StudyFlow</h1>
    </section>
  );
};

export default HomePage;
